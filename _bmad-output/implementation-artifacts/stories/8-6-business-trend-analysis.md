# Story 8.6: 业务趋势分析

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

**As a** 总监或管理员  
**I want to** 查看业务趋势分析（订单量趋势、客户增长趋势等）  
**So that** 我可以预测未来业务走向，制定战略决策

## Acceptance Criteria

### AC1: 业务趋势分析基础显示
**Given** 总监或管理员已登录系统并访问"业务仪表板"  
**When** 总监或管理员点击"业务趋势分析"模块  
**Then** 系统显示关键业务指标的历史趋势图，例如：
- 订单量月度/季度/年度趋势（FR75）
- 客户增长趋势（新增客户数按时间）
- 销售额趋势（如果系统有订单数据）
**And** 分析结果通过图表和可视化方式展示（FR148）

### AC2: 趋势图交互功能
**Given** 总监或管理员查看业务趋势分析  
**When** 系统显示趋势图  
**Then** 系统显示折线图或面积图展示趋势  
**And** 系统支持选择时间范围（最近 7 天、最近 30 天、最近 3 个月、最近 1 年等）  
**And** 系统支持选择时间粒度（按天、按周、按月、按季度、按年）  
**And** 系统支持多指标对比（如同时显示订单量趋势和客户增长趋势）

### AC3: 趋势统计和预测
**Given** 总监或管理员查看业务趋势分析  
**When** 系统显示趋势数据  
**Then** 系统显示趋势统计（如"本月订单量较上月增长 10%"）  
**And** 系统显示趋势预测（可选，基于历史数据预测未来趋势）  
**And** 系统高亮显示关键时间节点（如峰值、低谷）

### AC4: 性能优化和数据导出
**Given** 总监或管理员查看业务趋势分析  
**When** 分析数据量很大（> 1000 条记录）  
**Then** 系统使用数据聚合优化性能（如按月聚合数据）  
**And** 系统支持导出分析结果（PDF、CSV、图片）

## Tasks / Subtasks

- [x] Task 1: 创建后端业务趋势分析 API (AC: 1,2,3)
  - [x] 1.1 创建业务趋势分析服务模块
    - 创建 `fenghua-backend/src/dashboard/business-trend-analysis.service.ts`
    - 注入 `PermissionService` 和 `ConfigService`
    - 实现业务趋势统计查询：
      - 查询订单量趋势（按时间粒度聚合）
      - 查询客户增长趋势（新增客户数按时间）
      - 查询销售额趋势（从订单金额汇总）
      - 计算趋势增长率（环比、同比）
      - 可选：实现趋势预测（基于历史数据）
    - 使用 `PermissionService.getDataAccessFilter(token)` 获取数据访问过滤器
    - 使用 PostgreSQL 聚合查询优化性能
    - 支持按时间范围筛选
    - 支持按时间粒度聚合（天、周、月、季度、年）
  - [x] 1.2 创建业务趋势分析控制器
    - 创建 `fenghua-backend/src/dashboard/business-trend-analysis.controller.ts`
    - 实现 `GET /api/dashboard/business-trend-analysis` 端点
    - 添加 JWT 认证和角色验证：`@UseGuards(JwtAuthGuard, DirectorOrAdminGuard)`
    - 支持查询参数：
      - `startDate` (可选): 开始日期（ISO 8601 格式）
      - `endDate` (可选): 结束日期（ISO 8601 格式）
      - `timeGranularity` (可选): 时间粒度（day/week/month/quarter/year），默认 month
      - `metrics` (可选): 指标列表（orderCount/customerGrowth/salesAmount），默认全部
    - 返回业务趋势数据：
      ```typescript
      {
        trends: Array<{
          period: string; // 时间周期（如 "2024-01"）
          orderCount: number;
          customerGrowth: number; // 新增客户数
          salesAmount: number;
          growthRate?: number; // 环比增长率（可选）
          yearOverYearGrowthRate?: number; // 同比增长率（可选）
        }>;
        summary: {
          totalOrderCount: number;
          totalCustomerGrowth: number;
          totalSalesAmount: number;
          averageGrowthRate: number;
        };
      }
      ```
    - 实现 `GET /api/dashboard/business-trend-analysis/forecast` 端点（可选）
      - 返回趋势预测数据（基于历史数据）
  - [x] 1.3 创建业务趋势分析 DTO
    - 创建 `fenghua-backend/src/dashboard/dto/business-trend-analysis.dto.ts`
    - 定义 `BusinessTrendAnalysisQueryDto`（查询参数）
    - 定义 `BusinessTrendItemDto`（单个趋势数据项）
    - 定义 `BusinessTrendResponseDto`（响应数据）
    - 定义 `TrendSummaryDto`（趋势汇总数据）
    - 使用 `class-validator` 验证输入参数
  - [x] 1.4 实现 Redis 缓存（可选，性能优化）
    - 缓存业务趋势分析查询结果（5 分钟过期）
    - 缓存键格式：`dashboard:business-trend-analysis:{filters}:{timeGranularity}`

- [x] Task 2: 创建前端业务趋势分析页面 (AC: 1,2,3)
  - [x] 2.1 创建业务趋势分析服务
    - 创建 `fenghua-frontend/src/dashboard/services/business-trend-analysis.service.ts`
    - 实现 `getBusinessTrendAnalysis` 函数（调用后端 API）
    - 实现 `getTrendForecast` 函数（获取趋势预测，可选）
    - 使用 React Query 进行数据获取和缓存
  - [x] 2.2 创建业务趋势图表组件
    - 创建 `fenghua-frontend/src/dashboard/components/BusinessTrendChart.tsx`
    - 使用 Recharts 显示趋势折线图或面积图
    - 支持多指标对比（多条折线）
    - 支持时间范围选择
    - 支持时间粒度选择
    - 高亮显示关键时间节点（峰值、低谷）
  - [x] 2.3 创建趋势统计组件
    - 创建 `fenghua-frontend/src/dashboard/components/TrendSummary.tsx`
    - 显示趋势统计信息（增长率、总量等）
    - 显示趋势预测（如果可用）
  - [x] 2.4 创建业务趋势分析页面
    - 创建 `fenghua-frontend/src/dashboard/pages/BusinessTrendAnalysisPage.tsx`
    - 集成时间范围选择器
    - 集成时间粒度选择器
    - 集成指标选择器（多选）
    - 集成业务趋势图表
    - 集成趋势统计组件
    - 实现加载状态和错误处理
    - 使用 React Query 的 `useQuery` 进行数据获取
    - 使用 `staleTime` 和 `gcTime` 优化缓存策略

- [x] Task 3: 添加路由和导航 (AC: 1)
  - [x] 3.1 添加业务趋势分析路由
    - 在 `fenghua-frontend/src/App.tsx` 中添加路由：`/dashboard/business-trend-analysis`
    - 使用 `ProtectedRoute` 组件，限制只有 `ADMIN` 和 `DIRECTOR` 角色可以访问
  - [x] 3.2 添加导航菜单项
    - 在 `fenghua-frontend/src/components/layout/MainLayout.tsx` 中添加"业务趋势分析"菜单项
    - 菜单项仅对 `ADMIN` 和 `DIRECTOR` 角色可见
    - 菜单项图标：📈

- [x] Task 4: 实现导出功能 (AC: 4)
  - [x] 4.1 后端导出功能
    - 在 `business-trend-analysis.controller.ts` 中实现 `GET /api/dashboard/business-trend-analysis/export` 端点
    - 支持导出格式：CSV、PDF（可选）、图片（PNG/JPEG，可选）
    - 导出数据包含所有趋势数据
    - 导出文件名包含当前日期和时间粒度
  - [x] 4.2 前端导出功能
    - 在 `BusinessTrendAnalysisPage.tsx` 中添加"导出"按钮
    - 实现导出功能，调用后端导出 API
    - 处理导出错误和加载状态
    - 支持导出格式选择（CSV、PDF、图片）

- [x] Task 5: 性能优化
  - [x] 5.1 数据库索引优化
    - 创建迁移文件：`027-add-business-trend-analysis-indexes.sql`
    - 添加索引优化趋势分析查询（按时间聚合）
  - [x] 5.2 前端性能优化
    - 使用 React.lazy 和 Suspense 懒加载图表组件
    - 使用 React Query 的 `staleTime` 和 `gcTime` 优化缓存
    - 实现请求重试机制（使用 React Query 的 `retry` 配置）
    - 对于大量数据，使用数据聚合（在服务端完成）

- [x] Task 6: 测试和文档
  - [x] 6.1 创建手动测试指南
    - 创建 `_bmad-output/test-reports/story-8-6-manual-testing-guide.md`
    - 包含所有测试用例和验证步骤
  - [x] 6.2 更新 sprint-status.yaml
    - 将 `8-6-business-trend-analysis` 状态更新为 `review`

## Dev Notes

### 业务趋势计算逻辑

#### 订单量趋势
- 按时间粒度聚合订单数量（`interaction_type` 为 `order_signed` 或 `order_completed`）
- 计算环比增长率：`(本期订单量 - 上期订单量) / 上期订单量 × 100%`
- 计算同比增长率：`(本期订单量 - 去年同期订单量) / 去年同期订单量 × 100%`

#### 客户增长趋势
- 按时间粒度统计新增客户数（基于 `companies.created_at`）
- 区分采购商和供应商的增长趋势
- 计算客户增长率

#### 销售额趋势
- 按时间粒度汇总订单金额（从 `additional_info` JSONB 字段中提取）
- 计算销售额环比和同比增长率

#### 趋势预测（可选）
- 基于历史数据使用简单线性回归或移动平均法预测未来趋势
- 预测周期：未来 3-6 个月

### 时间粒度处理

- **按天（day）**: 使用 `DATE_TRUNC('day', interaction_date)`
- **按周（week）**: 使用 `DATE_TRUNC('week', interaction_date)`
- **按月（month）**: 使用 `DATE_TRUNC('month', interaction_date)`
- **按季度（quarter）**: 使用 `DATE_TRUNC('quarter', interaction_date)`
- **按年（year）**: 使用 `DATE_TRUNC('year', interaction_date)`

### SQL 查询优化

- 使用 CTE（Common Table Expression）优化复杂查询
- 使用 `FILTER` 子句优化聚合查询
- 对于大量数据，在服务端进行聚合，减少传输数据量
- 使用数据库索引优化时间范围查询

### 参考实现

- 参考 Story 8.1（业务仪表板概览）的实现模式
- 参考 Story 8.2（产品关联分析）的图表实现
- 参考 Story 8.3（客户分析）的趋势图实现
- 保持代码风格和架构一致性

### 图表库选择

- 使用 Recharts（与现有分析页面保持一致）
- 支持折线图和面积图
- 支持多指标对比（多条折线）
- 支持交互功能（悬停显示详情、缩放等）

### 性能考虑

- 对于超过 1 年的数据，默认使用月度聚合
- 对于超过 3 个月的数据，默认使用周度聚合
- 使用 Redis 缓存查询结果（5 分钟过期）
- 前端使用虚拟滚动或分页（如果数据点过多）

## References

- [Source: _bmad-output/epics.md#Story-8.6]
- [Source: _bmad-output/implementation-artifacts/stories/8-1-business-dashboard-overview.md]
- [Source: _bmad-output/implementation-artifacts/stories/8-2-product-association-analysis.md]
- [Source: _bmad-output/implementation-artifacts/stories/8-3-customer-analysis.md]

